# Ignore local environment file with secrets
.env

# Python cache and env
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
.venv/

# Data/artifacts: runtime index files (bm25/faiss/chunks/sentences) are
# committed so the Streamlit Cloud deploy has data to load. Only the large
# build-time-only embedding cache stays ignored.
data/*.embcache.pkl

ARAG_IMPLEMENTATION.MD

